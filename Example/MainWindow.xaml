<Window x:Class="Example.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mcf="clr-namespace:Craften.Utilities.MinecraftFormatting;assembly=Craften.Utilities.MinecraftFormatting"
        Title="Example"
        Height="350"
        Width="525">
    <StackPanel Orientation="Vertical">
        <mcf:MinecraftTextBlock Text="§K123§r §o§4Th§ce C§6ra§eft§aen§2 Se§3rv§1er §r§k123"
                                FontSize="72"
                                FontFamily="Lucida Console"
                                VerticalAlignment="Center"
                                x:Name="test"
                                TextAlignment="Center" />
        <TextBox Text="{Binding Text, ElementName=test, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
    </StackPanel>
</Window>
